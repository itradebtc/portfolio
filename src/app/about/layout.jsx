import React from 'react'

export const metadata = {
    title: 'About Me',
    description: 'About Kelubia Urubusi',
  }

export default function layout({children}) {
  return (
    <div>
        {children}
    </div>
  )
}