import React from 'react'

export const metadata = {
    title: 'Contact Me',
    description: 'Contact me on any jobs',
  }

export default function layout({children}) {
  return (
    <div>
        {children}
    </div>
  )
}